interface:
  display_name: "Style Audit"
  short_description: "Audit branch-diff Rust coding style"
  default_prompt: "Use $style-audit to audit branch-diff Rust files with the default `tools/style_audit.rs` origin/main base, or explicit file/directory targets with `--force-path`, and report style violations."
