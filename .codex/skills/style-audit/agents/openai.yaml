interface:
  display_name: "Style Audit"
  short_description: "Audit staged or branch-diff Rust coding style"
  default_prompt: "Use $style-audit to audit staged Rust files by default, or branch-diff Rust files with `tools/style_audit.rs --diff-base origin/main` during task resolve, and report style violations."
